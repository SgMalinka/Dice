export enum GuessType {
    OVER = 'Over',
    UNDER = 'Under',
}

export enum Number {
    HIGHER = 'higher',
    LOWER = 'lower',
}

export enum Status {
    SUCCESS = 'success',
    ERROR = 'error',
}
